<% title "وظائف للسيدات فقط"%>
<div class="search-job-filter">
  <div class="content">
      <h2>وظائف للسيدات فقط</h2>
  <div class="row">
        <%= search_form_for @q do |f| %>
      <div class="col-md-1"><p>بحث حسب:</p></div>
    <div class="col-md-10">
<div class="col-md-3">
              <%= f.search_field :place_cont, :class=>"form-control", :placeholder=>"المدينة" %>
</div>
        <div class="col-md-3">
          <%= f.search_field :field_cont, :class=>"form-control", :placeholder=>"المجال" %>
        </div>

        <div class="col-md-3">
          <%= f.select(:applicant_status_cont, [["خريج حديث", "fresh-graduate"], ["صاحب خبرة","experienced"]], {include_blank: 'الحالة'}, {:class =>'form-control job_condition'})%>
        </div>

        <div class="col-md-3">
          <%= f.search_field :description_cont, :class=>"form-control", :placeholder=>"بحث متقدم" %>
        </div>
</div>
<div class="col-md-1"><%= f.submit "بحث", :class => "btn btn-block btn-danger" %></div>
      <%end%>
  </div>
</div>
</div>

  <div class="content job-filter">

    <% if @jobs.count > 0 %>
      <div class="row">
      <!-- Three columns of text below the carousel -->
      <div class="row job-list">

      <% @jobs.each do |job| %>
          <% if job.sex == "women" %>
          <% employer = job.employer %>
        <div class="col-md-3">
                <div class="job-item">
          <div class="row">
            <div class="col-md-3">
              <div class="job-pic"><% if job.employer.avatar? %> <%= image_tag job.employer.avatar.url(:medium) %> <% else %> <i class="fa fa-user"></i> <%end%></div>
            </div>
            <div class="col-md-8">
              <div class="job-title">
                <a href="<%= job_path(employer) %>" class="employee"><% if job.sex == "male" %> <i class="fa fa-male"></i> <% elsif job.sex == "female" %> <i class="fa fa-female"></i> <% end %> <%= job.title %></a>
                <a href=" <%= employer_path(employer) %>" class="employer danger"><%= job.employer.co_name %></a>
              </div>
            </div>
          </div>
          <div class="row">
            <div class="job-conditions col-md-12">
              <p><%= job.place %></p>
              <p><%= job.experience %></p>
            </div>
          </div>
          <div class="row">
            <div class="job-description">
                <p> <%= job.description %> </p>
            </div>
          </div>
          <div class="row">
            <% if applicant_signed_in? %>
             <div class="col-md-6"> <%= link_to "قدم",  job_create_follower_path(job), :method => :post, :class => "btn btn-block btn-primary" %></div>
              <% elsif employer_signed_in? %>
                <% if job.employer == current_employer %>
                    <div class="col-md-6"><%= link_to "تعديل", edit_job_path(job), :class => "btn btn-block btn-primary" %></div>
                    <%end%>
              <% else %>

                <div class="col-md-6"> <%= link_to "قدم", new_applicant_registration_path, :class => "btn btn-block btn-primary" %></div>
              <%end%>
            <div class="col-md-6"> <div class="btn btn-block btn-primary">مشاركة</div></div>
          </div>
        </div>
        </div><!-- /job item -->

            <%end%>
          <%end%>
    </div>
    </div>
    <% else %>
          <div class="empty-content">
            <h1>لم تسجل أي وظيفة بعد. <%= link_to "أضف وظيفة جديدة", new_job_path %></h1>
          </div>
    <%end%>


  </div>
</div>
